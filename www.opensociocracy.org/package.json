{
  "scripts": {
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs",
    "dev:dump": "export PGPASSWORD=opensociocracy_postgres_root_pass && sh ./docker-postgres/scripts/dump.sh",
    "dev:postman": "newman run postman/Automated.postman_collection.json -e  postman/LOCAL.postman_environment.json"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.78.0",
    "vitepress": "^1.0.0-alpha.75"
  }
}
